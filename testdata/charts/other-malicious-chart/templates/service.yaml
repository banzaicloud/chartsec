apiVersion: v1
kind: Service
metadata:
  name: {{ include "other-malicious-chart.fullname" . }}
  labels:
    app.kubernetes.io/name: {{ include "other-malicious-chart.name" . }}
    helm.sh/chart: {{ include "other-malicious-chart.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "other-malicious-chart.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
